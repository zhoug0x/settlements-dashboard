import { DefaultTheme } from 'styled-components';

// When adding values to the theme, update 'DefaultTheme' interface in 'styled.d.ts'
const theme: DefaultTheme = {
	colors: {
		text: '#333',
		background: '#FFF',
		link: 'cornflowerblue',
		primary: '#333',
		secondary: '#555',
	},
};

export default theme;
